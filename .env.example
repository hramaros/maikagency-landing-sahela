# URL du webhook n8n qui traite les réservations.
# Workflow : Webhook → Data Table « Réservations Sahela » → email salon + confirmation cliente.
# Si cette variable est absente, l'instance de production ci-dessous est utilisée par défaut.
N8N_WEBHOOK_URL=https://n8n.maikagency.dev/webhook/sahela-reservation
